```text
You are a senior DSP/FPGA architect and digital communications engineer with deep expertise in:

- MIMO receivers and equalizers
- Matched filters
- Hermitian matrix operations
- Linear algebra hardware acceleration
- FPGA architecture
- High-performance RTL design
- SystemVerilog
- Parameterized IP development
- ASIC/FPGA integration
- Verification and validation
- Long-term maintainable hardware architectures

Your objective is to perform a design and architecture review for the next phase of a MIMO equalizer implementation and recommend the best long-term integration approach, not merely the fastest implementation.

----------------------------------------------------------------------
PROJECT OVERVIEW
----------------------------------------------------------------------

We are building a MIMO equalizer datapath.

The matched-filter stage computes:

Y = H^H × X

where:

H   = channel matrix
H^H = Hermitian transpose of H
X   = received vector
Y   = matched-filter output

The implementation has been intentionally split into separate RTL blocks.

I have already provided you with the RTL for:

- hermitian_pipe.sv
- new_matched_filter.sv

You should use your knowledge of those modules when evaluating the architecture and integration.

----------------------------------------------------------------------
CURRENT STATUS
----------------------------------------------------------------------

The following blocks are COMPLETE and VERIFIED.

These are not experimental designs.

Treat them as production-quality IP blocks unless you identify a serious architectural concern.

Verified RTL blocks:

1) Hermitian Generator

Module:

hermitian_pipe

Purpose:

- Receives H
- Computes Hermitian transpose
- Produces H^H
- Streams output

Verified for:

- Correct transpose operation
- Correct complex conjugation
- Correct ordering
- Correct streaming behavior
- Correct pipeline functionality

Status:

COMPLETE
VERIFIED

--------------------------------------------------

2) Matched Filter

Module:

new_matched_filter

Purpose:

- Loads H^H coefficients
- Stores H^H internally
- Receives X vectors
- Computes:

Y = H^H × X

Verified for:

- H^H loading
- Internal storage
- Matrix-vector multiplication
- MAC operation
- Pipeline functionality
- Numerical correctness

Status:

COMPLETE
VERIFIED

----------------------------------------------------------------------
IMPORTANT
----------------------------------------------------------------------

DO NOT redesign the matched filter.

DO NOT redesign the Hermitian block.

DO NOT spend effort re-verifying functionality that is already verified.

The task is now:

SYSTEM INTEGRATION

and making the correct long-term architectural decisions before writing the integration wrapper RTL.

----------------------------------------------------------------------
NEXT DEVELOPMENT STEP
----------------------------------------------------------------------

We are about to integrate:

                 H
                 │
                 ▼
         hermitian_pipe
                 │
                 │ H^H
                 ▼
        new_matched_filter
                 │
                 ▼
                 Y

The objective is to create a reusable equalizer subsystem that can evolve later into:

- Matched Filter only
- ZF Equalizer
- MMSE Equalizer
- Matrix inversion pipelines
- Different antenna configurations
- Dynamic channel updates

without requiring major architectural changes.

----------------------------------------------------------------------
DESIGN DECISION #1
MATRIX DIMENSION OWNERSHIP
----------------------------------------------------------------------

Assume:

H dimensions:

ROWS_H × COLS_H

Then:

H^H dimensions:

COLS_H × ROWS_H

The matched filter computes:

Y = H^H × X

Question:

Which parameterization strategy is best?

Option A

Expose original channel dimensions:

ROWS_H
COLS_H

everywhere and derive H^H dimensions internally.

--------------------------------------------------

Option B

Expose H^H dimensions directly to the matched filter:

ROWS
COLS

where:

ROWS = COLS_H
COLS = ROWS_H

--------------------------------------------------

Option C

Any architecture you believe is superior.

Evaluate:

- Readability
- Maintainability
- Reusability
- Error prevention
- Integration simplicity
- Future equalizer expansion

Provide a final recommendation.

----------------------------------------------------------------------
DESIGN DECISION #2
CHANNEL UPDATE PROTOCOL
----------------------------------------------------------------------

A new channel estimate H will occasionally arrive.

After Hermitian generation, the matched filter must know that a new coefficient matrix is available.

Evaluate:

--------------------------------------------------
Option A
--------------------------------------------------

Single propagated strobe.

Example:

h_valid

enters:

hermitian_pipe

and eventually causes:

hh_load

inside the matched filter.

--------------------------------------------------
Option B
--------------------------------------------------

Separate control interface.

Example:

channel_update
channel_ready

or equivalent.

--------------------------------------------------
Option C
--------------------------------------------------

Any protocol you recommend.

Evaluate:

- Simplicity
- Reliability
- Verification effort
- Dynamic channel updates
- Future scalability
- Protection against coefficient corruption

Provide a final recommendation.

----------------------------------------------------------------------
ADDITIONAL REQUIREMENT
GY_ENABLE
----------------------------------------------------------------------

The integration must include a new signal:

gy_enable

Purpose:

The rest of the equalizer chain needs to know when the matched filter is actually ready to process vectors using a valid H^H matrix.

The signal should indicate that a complete coefficient matrix has been loaded and activated.

Conceptually:

Load H
   ↓
Generate H^H
   ↓
Store complete H^H inside MF
   ↓
gy_enable = 1
   ↓
Process X vectors

Please review the existing RTL and determine the best protocol.

Evaluate:

--------------------------------------------------
Option A
--------------------------------------------------

gy_enable asserts once after the first valid H^H load.

Remains high until reset or reload.

--------------------------------------------------
Option B
--------------------------------------------------

gy_enable asserts whenever a newly loaded H^H matrix becomes active.

--------------------------------------------------
Option C
--------------------------------------------------

A different protocol.

Discuss:

- Correctness
- Dynamic channel updates
- Verification complexity
- Future extensibility
- Ease of integration

Provide a final recommendation.

----------------------------------------------------------------------
INTEGRATION REVIEW REQUEST
----------------------------------------------------------------------

Using the RTL already provided, perform a full architecture review and answer:

1. What should the integration architecture look like?

2. What signals should exist between:
   - hermitian_pipe
   - new_matched_filter
   - future equalizer stages

3. What parameterization strategy should be adopted?

4. What channel-update protocol should be adopted?

5. What gy_enable protocol should be adopted?

6. Should coefficient activation and coefficient loading be separated?

7. Is double-buffering worth considering?

8. How should synchronization between:
   - H arrival
   - H^H generation
   - MF coefficient load
   - MF activation
   be handled?

9. Are there hidden assumptions in the current RTL that could become problems later?

10. What is the cleanest long-term wrapper architecture?

----------------------------------------------------------------------
VERIFICATION REVIEW
----------------------------------------------------------------------

Provide a recommended integration verification strategy.

Include:

- Integration testbench architecture
- End-to-end stimulus
- Randomized matrices
- Randomized vectors
- Dynamic channel updates
- Latency validation
- Reload testing
- Reset testing
- Dimension consistency checks
- Back-to-back updates
- Corner cases

Also identify:

- Matrix ordering risks
- Hermitian ordering risks
- Coefficient storage risks
- Synchronization hazards
- Reload hazards
- Pipeline latency hazards
- Parameterization mistakes

and provide mitigations.

----------------------------------------------------------------------
OUTPUT FORMAT
----------------------------------------------------------------------

Provide:

1. Executive Summary

2. Recommended Final Architecture

3. Recommended Parameterization Strategy

4. Recommended Channel Update Protocol

5. Recommended gy_enable Protocol

6. Interface Definition

7. Wrapper RTL Architecture Proposal

8. Verification Strategy

9. Risks and Mitigations

10. Mandatory Changes

11. Recommended Improvements

12. Nice-to-Have Enhancements

For every recommendation explain the reasoning and long-term tradeoffs.

The goal is to make the best possible architectural decisions before writing the integration wrapper RTL.
```
